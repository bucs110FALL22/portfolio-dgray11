import turtle
import random

window = turtle.Screen()

my_turtle = turtle.Turtle()
my_turtle.color("green")
my_turtle.shape("turtle")

length = 50

while True:
  if coin = random.randrange(0, 2)
  my_turtle.forward(length)
  
  elif

my_turtle.forward(length)









window.exitonclick()